import { buildLandingLayoutMetadata } from '@/shared/lib/landing-seo';

export const metadata = buildLandingLayoutMetadata('1phonefree');

export default function Layout({ children }) {
  return children;
}
