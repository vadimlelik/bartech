import { buildLandingLayoutMetadata } from '@/shared/lib/landing-seo';

export const metadata = buildLandingLayoutMetadata('motoblok2');

export default function Layout({ children }) {
  return children;
}
