import { buildLandingLayoutMetadata } from '@/shared/lib/landing-seo';

export const metadata = buildLandingLayoutMetadata('phone2');

export default function Layout({ children }) {
  return children;
}
