import { buildLandingLayoutMetadata } from '@/shared/lib/landing-seo';

export const metadata = buildLandingLayoutMetadata('phone3');

export default function Layout({ children }) {
  return children;
}
