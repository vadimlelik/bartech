export const metadata = {
  title: 'Админ-панель | Texnobar',
  description: 'Панель администрирования интернет-магазина Texnobar.',
  robots: {
    index: false,
    follow: false,
  },
};

export default function AdminLayout({ children }) {
  return children;
}
