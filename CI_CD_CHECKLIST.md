# CI/CD Проверка - Чеклист работоспособности

## ✅ Проверенные компоненты

### 1. Workflow файлы
- ✅ `.github/workflows/docker-build-push.yml` - Корректно настроен
- ✅ `.github/workflows/deploy.yml` - Корректно настроен, добавлены permissions

### 2. Docker конфигурация
- ✅ `Dockerfile` - Multi-stage build, оптимизирован для production
- ✅ `docker-compose.yml` - Базовый файл с сервисами
- ✅ `docker-compose.prod.yml` - Production override, использует образ из Docker Hub
- ✅ `.dockerignore` - Правильно исключает ненужные файлы

### 3. Health Check
- ✅ `/api/health` endpoint существует в `src/app/api/health/route.js`
- ✅ Health check используется в docker-compose healthcheck
- ✅ Health check используется в deploy workflow для проверки готовности

### 4. Имена контейнеров и сервисов
- ✅ Service name: `nextjs` (используется в docker-compose exec)
- ✅ Container name: `bartech-nextjs` (используется в docker rm)
- ✅ Все имена согласованы между файлами

### 5. Docker Volumes
- ✅ `technobar_certbot-etc` - создается автоматически при деплое
- ✅ `technobar_certbot-var` - создается автоматически при деплое
- ✅ Volumes правильно подключены в docker-compose.yml

## 🔑 Требуемые Secrets в GitHub

### Для docker-build-push.yml:
1. `DOCKERHUB_USERNAME` - Имя пользователя Docker Hub
2. `DOCKERHUB_TOKEN` - Токен доступа Docker Hub
3. `NEXT_PUBLIC_SUPABASE_URL` - (опционально) URL Supabase
4. `NEXT_PUBLIC_SUPABASE_ANON_KEY` - (опционально) Anon key Supabase

### Для deploy.yml:
1. `SSH_USER` - Пользователь для SSH подключения к серверу
2. `SERVER_HOST` - IP адрес или домен сервера
3. `SSH_PRIVATE_KEY` - Приватный SSH ключ для подключения

## 📋 Требуемые переменные в .env на сервере

На сервере в `/opt/bartech/.env` должны быть:
1. `DOCKERHUB_USERNAME` - **ОБЯЗАТЕЛЬНО**
2. `NEXT_PUBLIC_SUPABASE_URL` - Рекомендуется
3. `NEXT_PUBLIC_SUPABASE_ANON_KEY` - Рекомендуется
4. `SUPABASE_SERVICE_ROLE_KEY` - Для админ функций

## 🔄 Процесс CI/CD

### 1. Build Workflow (docker-build-push.yml)
**Триггеры:**
- Push в ветки `main` или `master`
- Push тегов `v*`
- Pull Request в `main` или `master` (только сборка, без push)

**Действия:**
1. Checkout кода
2. Настройка Docker Buildx
3. Логин в Docker Hub (кроме PR)
4. Сборка и push образа с тегами
5. Верификация сборки

### 2. Deploy Workflow (deploy.yml)
**Триггеры:**
- Автоматически после успешного завершения "Build and Push Docker Image"
- Ручной запуск через `workflow_dispatch`

**Действия:**
1. Checkout кода
2. Настройка SSH подключения
3. Подключение к серверу и деплой:
   - Проверка .env файла
   - Создание Docker volumes (если нужно)
   - Pull нового образа из Docker Hub
   - Остановка старых контейнеров
   - Запуск новых контейнеров
   - Ожидание готовности Next.js (health check)
   - Перезагрузка Nginx
   - Очистка старых образов
4. Финальная проверка health check через HTTPS

## 🔄 Последние улучшения (обновление образов)

### Проблема: После деплоя не обновляются данные на странице
**Решение:** Улучшен процесс обновления образов в deploy workflow:
1. ✅ Удаление всех старых образов перед pull
2. ✅ Очистка кеша Docker builder
3. ✅ Принудительный pull нового образа из registry
4. ✅ Проверка, что образ действительно обновился (сравнение ID)
5. ✅ Пересоздание контейнеров с новым образом
6. ✅ Очистка кеша Next.js внутри контейнера
7. ✅ Проверка, что контейнер использует правильный образ

**Как это работает:**
- При каждом деплое workflow сохраняет ID старого образа
- Удаляет все старые образы bartech
- Загружает новый образ из Docker Hub
- Сравнивает ID старого и нового образа
- Если ID не изменился, выводит предупреждение
- Пересоздает контейнеры с новым образом
- Очищает кеш Next.js

## ⚠️ Потенциальные проблемы и решения

### Проблема: Workflow не запускается автоматически
**Решение:** Убедитесь, что:
- Оба workflow файла находятся в `.github/workflows/`
- Имя workflow точно совпадает: "Build and Push Docker Image"
- Ветка по умолчанию - `main` или `master`

### Проблема: Не удается подключиться к серверу
**Решение:** Проверьте:
- `SSH_PRIVATE_KEY` секрет правильно настроен
- `SSH_USER` и `SERVER_HOST` корректны
- SSH ключ добавлен в authorized_keys на сервере
- Сервер доступен из интернета

### Проблема: Образ не найден при pull
**Решение:** Убедитесь, что:
- `DOCKERHUB_USERNAME` правильный в secrets и .env
- Образ был успешно собран и запушен в Docker Hub
- Права доступа к репозиторию Docker Hub корректны

### Проблема: Health check не проходит
**Решение:** Проверьте:
- Next.js контейнер запущен и работает
- Порты 80 и 443 открыты на сервере
- SSL сертификаты настроены правильно
- Nginx конфигурация корректна

### Проблема: Данные не обновляются после деплоя
**Решение:** 
1. Проверьте логи деплоя - должен быть вывод о сравнении ID образов
2. Убедитесь, что образ действительно обновился в Docker Hub
3. Проверьте, что контейнер использует новый образ: `docker inspect bartech-nextjs | grep Image`
4. Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
5. Проверьте, что в build workflow образ был успешно запушен с тегом `latest`
6. Если проблема сохраняется, проверьте логи Next.js: `docker logs bartech-nextjs`

### Проблема: Контейнеры не перезапускаются
**Решение:** В deploy.yml уже есть:
- `--force-recreate` флаг
- Удаление старых контейнеров перед запуском
- Принудительный pull образа

## 🧪 Тестирование CI/CD

### Ручной тест деплоя:
1. Перейдите в GitHub Actions
2. Выберите workflow "Deploy to Production"
3. Нажмите "Run workflow"
4. Выберите ветку и запустите

### Проверка после деплоя:
1. Проверьте статус контейнеров: `docker ps`
2. Проверьте логи: `docker-compose logs nextjs`
3. Проверьте health endpoint: `curl https://technobar.by/api/health`
4. Проверьте работу сайта в браузере

## 📝 Примечания

- Workflow использует `workflow_run` для автоматического деплоя после сборки
- Health check endpoint возвращает статус 200 с информацией о времени работы
- Docker volumes создаются автоматически при первом деплое
- Старые образы автоматически очищаются после деплоя
- Nginx автоматически перезагружается после готовности Next.js

## ✅ Итоговая проверка

Все компоненты CI/CD настроены корректно и готовы к работе. Убедитесь, что:
1. ✅ Все secrets настроены в GitHub
2. ✅ .env файл существует на сервере с правильными переменными
3. ✅ SSH доступ к серверу работает
4. ✅ Docker и docker-compose установлены на сервере
5. ✅ Порты 80 и 443 открыты на сервере

