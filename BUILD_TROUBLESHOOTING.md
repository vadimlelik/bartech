# 🔧 Решение проблем со сборкой Next.js

## Проблема: Сборка зависает на этапе "Creating an optimized production build"

### ✅ Решения, которые уже применены:

1. **Увеличен лимит памяти Node.js до 8GB** в Dockerfile
2. **Добавлены дополнительные флаги Node.js** для стабильности
3. **Увеличен shared memory** для Docker build процесса
4. **Добавлены лимиты памяти** для контейнера в docker-compose.yml

### 🚀 Дополнительные шаги для решения проблемы:

#### 1. Проверьте доступную память на сервере

```bash
# Проверьте доступную память
free -h

# Проверьте использование памяти Docker
docker system df
docker stats
```

**Требования:**
- Минимум 8GB RAM для сборки
- Рекомендуется 16GB RAM для комфортной работы

#### 2. Очистите Docker кеш и неиспользуемые ресурсы

```bash
# Полная очистка (используйте осторожно!)
make clean-rebuild

# Или вручную:
docker system prune -a --volumes
docker builder prune -a
```

#### 3. Увеличьте лимиты Docker (если используете Docker Desktop)

**Для Docker Desktop (macOS/Windows):**
1. Откройте Docker Desktop → Settings → Resources
2. Увеличьте Memory до минимум 8GB (рекомендуется 12GB+)
3. Увеличьте Swap до 2GB
4. Нажмите "Apply & Restart"

**Для Linux:**
```bash
# Проверьте текущие лимиты
docker info | grep -i memory

# Если нужно, настройте systemd для Docker
sudo systemctl edit docker
# Добавьте:
# [Service]
# MemoryLimit=16G
```

#### 4. Используйте сборку с увеличенными лимитами

```bash
# Сборка с увеличенными лимитами памяти
DOCKER_BUILDKIT=1 docker-compose build --no-cache --progress=plain nextjs

# Или через Makefile
make clean-rebuild
```

#### 5. Мониторинг процесса сборки

Во время сборки в другом терминале:

```bash
# Следите за использованием памяти
watch -n 1 'docker stats --no-stream'

# Или проверьте логи сборки
docker-compose build --progress=plain nextjs 2>&1 | tee build.log
```

#### 6. Оптимизация Next.js конфигурации

Если проблема сохраняется, можно временно отключить некоторые оптимизации:

В `next.config.mjs` можно добавить:

```javascript
const nextConfig = {
  // ... существующие настройки
  swcMinify: true, // Использовать SWC вместо Terser (быстрее)
  compiler: {
    removeConsole: process.env.NODE_ENV === 'production',
  },
  // Отключить source maps для ускорения сборки (только для production)
  productionBrowserSourceMaps: false,
}
```

#### 7. Проверьте размер проекта

```bash
# Проверьте размер директорий
du -sh src/
du -sh public/
du -sh node_modules/

# Если public/ слишком большой, рассмотрите использование CDN для изображений
```

#### 8. Используйте BuildKit для более эффективной сборки

```bash
# Убедитесь, что BuildKit включен
export DOCKER_BUILDKIT=1
export COMPOSE_DOCKER_CLI_BUILD=1

# Затем соберите
docker-compose build nextjs
```

#### 9. Альтернативный подход: Сборка на более мощной машине

Если сервер слабый, можно:
1. Собрать образ локально на более мощной машине
2. Запушить в Docker Hub
3. Использовать `make force-update` на сервере

#### 10. Проверьте логи на ошибки

```bash
# Соберите с подробными логами
docker-compose build --progress=plain --no-cache nextjs 2>&1 | tee build-debug.log

# Ищите ошибки памяти
grep -i "memory\|killed\|oom" build-debug.log

# Ищите ошибки Next.js
grep -i "error\|failed\|warning" build-debug.log
```

### 🔍 Диагностика проблемы

#### Проверка 1: Достаточно ли памяти?

```bash
# Во время сборки проверьте
docker stats bartech-nextjs
```

Если использование памяти близко к лимиту → увеличьте лимиты

#### Проверка 2: Есть ли ошибки в логах?

```bash
# Проверьте логи сборки
docker-compose logs nextjs | tail -100
```

#### Проверка 3: Зависает ли процесс?

```bash
# Проверьте активные процессы сборки
docker ps -a | grep nextjs
docker top <container_id>
```

### 📝 Временное решение: Упрощенная сборка

Если ничего не помогает, можно временно отключить некоторые оптимизации:

В `Dockerfile` временно замените:
```dockerfile
RUN npm run build
```

На:
```dockerfile
# Временное решение: сборка без оптимизаций
RUN NEXT_TELEMETRY_DISABLED=1 NODE_OPTIONS="--max_old_space_size=8192" npm run build -- --debug
```

### ⚠️ Важные замечания

1. **Не прерывайте сборку** - это может повредить кеш
2. **Используйте `--no-cache`** только при необходимости (замедляет сборку)
3. **Проверьте .dockerignore** - убедитесь, что не копируются лишние файлы
4. **Мониторьте использование диска** - сборка может занимать много места

### 🆘 Если ничего не помогает

Если сборка постоянно зависает на сервере из-за нехватки памяти, соберите образ локально на более мощной машине и опубликуйте в Docker Hub.

#### Вариант 1: Использование готового скрипта (рекомендуется)

```bash
# На локальной машине с достаточным количеством RAM (8GB+)
./scripts/build-and-push-local.sh
```

Скрипт автоматически:
- Проверит наличие .env файла и необходимых переменных
- Проверит авторизацию в Docker Hub
- Соберет образ с правильными build arguments
- Опубликует образ в Docker Hub
- Покажет инструкции для обновления на сервере

#### Вариант 2: Ручная сборка и публикация

**Шаг 1: Подготовка на локальной машине**

1. Убедитесь, что у вас установлен Docker и достаточно RAM (8GB+)
2. Склонируйте репозиторий или скопируйте проект на локальную машину
3. Создайте `.env` файл с необходимыми переменными:
   ```bash
   DOCKERHUB_USERNAME=yourusername
   NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   ```

**Шаг 2: Авторизация в Docker Hub**

```bash
docker login
# Введите ваш username и password (или access token)
```

**Шаг 3: Сборка образа**

```bash
# Перейдите в директорию проекта
cd /path/to/bartech

# Загрузите переменные окружения
export $(cat .env | grep -v '^#' | xargs)

# Соберите образ
docker build \
  --tag ${DOCKERHUB_USERNAME}/bartech:latest \
  --build-arg NEXT_PUBLIC_SUPABASE_URL="${NEXT_PUBLIC_SUPABASE_URL}" \
  --build-arg NEXT_PUBLIC_SUPABASE_ANON_KEY="${NEXT_PUBLIC_SUPABASE_ANON_KEY}" \
  .
```

**Шаг 4: Публикация образа в Docker Hub**

```bash
docker push ${DOCKERHUB_USERNAME}/bartech:latest
```

**Шаг 5: Обновление на сервере**

Подключитесь к серверу и выполните:

```bash
# Перейдите в директорию проекта
cd /opt/bartech  # или ваш путь

# Убедитесь, что в .env установлен DOCKERHUB_USERNAME
# Затем обновите приложение
make force-update
```

Или вручную:

```bash
# Загрузите переменные из .env
export $(cat .env | grep -v '^#' | xargs)

# Остановите старые контейнеры
docker-compose -f docker-compose.yml -f docker-compose.prod.yml down

# Удалите старые образы
docker images ${DOCKERHUB_USERNAME}/bartech --format "{{.ID}}" | xargs -r docker rmi -f

# Загрузите новый образ
docker pull ${DOCKERHUB_USERNAME}/bartech:latest

# Запустите контейнеры
docker-compose -f docker-compose.yml -f docker-compose.prod.yml up -d --force-recreate
```

#### Проверка успешной публикации

После публикации проверьте, что образ доступен:

```bash
# Проверьте на Docker Hub
# https://hub.docker.com/r/YOUR_USERNAME/bartech/tags

# Или попробуйте загрузить образ
docker pull YOUR_USERNAME/bartech:latest
```

#### Требования для локальной сборки

- **RAM**: Минимум 8GB (рекомендуется 16GB+)
- **Docker**: Установлен и запущен
- **Интернет**: Для загрузки зависимостей и публикации образа
- **Docker Hub аккаунт**: С правами на создание репозиториев
- **.env файл**: С необходимыми переменными окружения

#### Альтернатива: Использование GitHub Actions

Если у вас есть доступ к GitHub Actions, можно запустить сборку там:

1. Запушьте код в репозиторий
2. GitHub Actions автоматически соберет и опубликует образ
3. Затем используйте `make force-update` на сервере

#### Устранение проблем

**Проблема: "unauthorized: authentication required"**
```bash
# Войдите в Docker Hub
docker login
```

**Проблема: "denied: requested access to the resource is denied"**
- Проверьте, что имя репозитория правильное: `username/bartech`
- Убедитесь, что репозиторий существует на Docker Hub (создайте его вручную, если нужно)

**Проблема: Сборка все еще зависает локально**
- Увеличьте лимит памяти Docker Desktop (Settings → Resources → Memory)
- Закройте другие приложения, освободите память
- Используйте более мощную машину

Или свяжитесь с поддержкой для диагностики конкретной проблемы.
