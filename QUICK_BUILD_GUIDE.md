# 🚀 Быстрая инструкция: Сборка и публикация образа локально

## Когда использовать

Используйте этот метод, если:
- ✅ Сборка на сервере постоянно зависает из-за нехватки памяти
- ✅ У вас есть локальная машина с достаточным количеством RAM (8GB+)
- ✅ Нужно быстро обновить приложение на сервере

## Быстрый старт

### На локальной машине:

```bash
# 1. Убедитесь, что в .env установлен DOCKERHUB_USERNAME
# 2. Запустите скрипт
make build-push-local

# Или напрямую:
./scripts/build-and-push-local.sh
```

Скрипт автоматически:
- ✅ Проверит все необходимые переменные
- ✅ Соберет образ с правильными параметрами
- ✅ Опубликует образ в Docker Hub
- ✅ Покажет инструкции для обновления на сервере

### На сервере:

```bash
# После успешной публикации образа
make force-update
```

## Требования

- **RAM**: Минимум 8GB (рекомендуется 16GB+)
- **Docker**: Установлен и запущен
- **Docker Hub**: Аккаунт с правами на создание репозиториев
- **.env файл**: С переменными `DOCKERHUB_USERNAME`, `NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`

## Что делать, если что-то пошло не так?

1. **Ошибка авторизации**: `docker login`
2. **Ошибка доступа к репозиторию**: Создайте репозиторий `bartech` на Docker Hub вручную
3. **Сборка зависает**: Увеличьте лимит памяти Docker Desktop (Settings → Resources → Memory)
4. **Подробные инструкции**: См. `BUILD_TROUBLESHOOTING.md`

## Альтернатива: GitHub Actions

Если у вас настроен CI/CD, просто запушьте код в репозиторий - GitHub Actions автоматически соберет и опубликует образ.
